// Code generated by Wire. DO NOT EDIT.

//go:generate go run github.com/google/wire/cmd/wire
//go:build !wireinject
// +build !wireinject

package main

import (
	"github.com/go-kratos/kratos/contrib/registry/nacos/v2"
	"github.com/go-kratos/kratos/v2"
	"github.com/go-kratos/kratos/v2/log"
	"github.com/the-zion/matrix-core/app/comment/service/internal/biz"
	"github.com/the-zion/matrix-core/app/comment/service/internal/conf"
	"github.com/the-zion/matrix-core/app/comment/service/internal/data"
	"github.com/the-zion/matrix-core/app/comment/service/internal/server"
	"github.com/the-zion/matrix-core/app/comment/service/internal/service"
)

// Injectors from wire.go:

// wireApp init kratos application.
func wireApp(confServer *conf.Server, confData *conf.Data, logLogger log.Logger, registry *nacos.Registry) (*kratos.App, func(), error) {
	db := data.NewDB(confData)
	client := data.NewCosServiceClient(confData)
	cmdable := data.NewRedis(confData)
	reviewMqPro := data.NewRocketmqReviewProducer(confData)
	commentMqPro := data.NewRocketmqCommentProducer(confData)
	achievementMqPro := data.NewRocketmqAchievementProducer(confData)
	creationClient := data.NewCreationServiceClient(registry)
	dataData, cleanup2, err := data.NewData(db, client, cmdable, reviewMqPro, commentMqPro, achievementMqPro, creationClient, logLogger)
	if err != nil {
		return nil, nil, err
	}
	commentRepo := data.NewCommentRepo(dataData, logLogger)
	recovery := data.NewRecovery(dataData)
	transaction := data.NewTransaction(dataData)
	commentUseCase := biz.NewCommentUseCase(commentRepo, recovery, transaction, logLogger)
	commentService := service.NewCommentService(commentUseCase, logLogger)
	httpServer := server.NewHTTPServer(confServer, commentService, logLogger)
	grpcServer := server.NewGRPCServer(confServer, commentService, logLogger)
	kratosApp := newApp(registry, httpServer, grpcServer)
	return kratosApp, func() {
		cleanup2()
	}, nil
}
